<h1><i class="fas fa-coffee"></i></span>Coffee Shop</h1>

<h2>Your order is ready!!</h2>

<ul>
  {{#each coffee}}
    {{#unless drink}}
      {{> coffee/coffee-block drink=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Your Coffee Order Line-up</h2>

<ul>
  {{#each coffee}}
    {{#if drink}}
      {{> coffee/coffee-block drink=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Make a New Coffee Order</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Coffee Flavour:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="drink">Drink?</label>
    <input type="radio" name="drink" value="1"> GULP it!<br>
    <input type="radio" name="drink" value="0"> Order

  </div>
  <button type="submit">Place Order</button>
</form>
